# Proyecto Streamlit + PyTorch + MongoDB

Este proyecto ejecuta una app de Streamlit con detección de anomalías usando PyTorch, y almacena resultados en MongoDB.

## 📦 Requisitos

- Docker
- Docker Compose

## 🚀 Cómo ejecutar

```bash
docker compose up --build
